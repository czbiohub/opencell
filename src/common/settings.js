
// the xy size of the z-stack
const zSliceSize = 600;

// the number of z-slices in the z-stack
const numZSlices = 27;

let apiUrl = API_URL;

export default {
    zSliceSize,
    numZSlices,
    apiUrl,
}
