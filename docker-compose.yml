version: '3.1'

services:

  dev_db:
    image: postgres
    restart: always
    container_name: opencelldb_container
    environment:
      POSTGRES_DB: opencelldb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    # 'bridge' is the default for docker run, but docker-compose requires it explicitly
    network_mode: "bridge" 
    ports:
      - 5433:5432 # out:in

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin_container
    # note that these two environment variables are required
    environment:
      PGADMIN_DEFAULT_EMAIL: keith.chev@gmail.com
      PGADMIN_DEFAULT_PASSWORD: password
    network_mode: "bridge"
    ports:
      - 8081:80
